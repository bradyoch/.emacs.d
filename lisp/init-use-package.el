;;; init-use-package.el --- Setup use-package for configuration  -*- lexical-binding: t; -*-
;;; Commentary:

;; This module sets up the use-package package which allows for more
;; declarative, easier to manage package management in conjunction
;; with straight.el.

;;; Code:

(straight-use-package 'use-package)

(provide 'init-use-package)
;;; init-use-package.el ends here
